SIMULATED — NOT CAPTURED FROM A RUNNING LAB
Status: SIMULATED-PASS
Purpose: expected OMP peer and TLOC state for a healthy three-edge overlay.

Expected command:
show omp peers

PEER             TYPE       STATE   UPTIME
1.1.1.3          vsmart     up      00:41:55

Expected command:
show omp tlocs

SYSTEM IP      SITE ID   COLOR             ENCAP   STATUS
1.1.1.5        200       mpls              ipsec   active
1.1.1.9        300       biz-internet      ipsec   active
1.1.1.13       400       public-internet   ipsec   active

Expected interpretation:
- one active vSmart peer per WAN Edge;
- one active transport TLOC per exported WAN Edge;
- Warsaw uses `mpls`;
- Madrid uses `biz-internet`;
- Sevilla uses `public-internet`.

Route limitation:
The embedded startup configs in the current .unl do not define complete service-side VPN routes for Warsaw/Madrid/Sevilla. Therefore this synthetic evidence does not invent `show omp routes` service prefixes that are not present in the public baseline.

A future runnable lab should populate service VPNs before OMP route verification is promoted to measured evidence.
